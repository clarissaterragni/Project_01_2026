name: VHDL CI (VUnit + GHDL)

on:
  push:
  pull_request:

jobs:
  vunit:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install GHDL
        run: |
          sudo apt update && sudo apt install -y ghdl

      - name: Install Python dependencies
        run: python -m pip install --upgrade pip && pip install vunit_hdl

      - name: Run VUnit tests
        run: python run.py --verbose 
